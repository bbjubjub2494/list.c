SPHINXOPTS    =
SPHINXBUILD   = sphinx-build
SOURCEDIR     = .
BUILDDIR      = _build

export PYTHONPATH

: |> PYTHONPATH=.:$$PYTHONPATH $(SPHINXBUILD) -M html "$(SOURCEDIR)" "$(BUILDDIR)" $(SPHINXOPTS) |> ^_build/

.gitignore